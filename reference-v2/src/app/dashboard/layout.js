export const metadata = {
    title: "Admin Dashboard",
    description: "Robotics Club AVV Admin Control Panel.",
    robots: { index: false, follow: false },
};

export default function DashboardLayout({ children }) {
    return children;
}
