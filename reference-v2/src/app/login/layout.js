export const metadata = {
    title: "Login",
    description: "Authenticate to access the Robotics Club AVV member portal and admin dashboard.",
    robots: { index: false, follow: false },
};

export default function LoginLayout({ children }) {
    return children;
}
